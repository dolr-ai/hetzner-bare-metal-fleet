[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hetzner-bare-metal-fleet"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "passlib",
    "jmespath",
    "netaddr",
]

[tool.setuptools]
packages = []
