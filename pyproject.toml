[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hetzner-bare-metal-fleet"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "passlib",
    "jmespath",
    "netaddr",
]

[tool.hatch.build.targets.wheel]
packages = []
